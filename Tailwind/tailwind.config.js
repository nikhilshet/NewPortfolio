/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./dist/*.html", // Include HTML files from the dist directory
  
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
